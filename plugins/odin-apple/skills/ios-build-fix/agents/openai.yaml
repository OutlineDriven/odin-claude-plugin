interface:
  display_name: "Ios Build Fix"
  short_description: "Use when asked to run /ios-build-fix to fix a failing iOS build, regenerate an Xcode project from project.yml via XcodeGen, or correct UI behavior through the debug bridge."
