interface:
  display_name: "Setup"
  short_description: "Use when the user asks to set up or configure the agent environment from a fresh or existing clone: credentials and placeholders (gathered into a non-overwriting .env, with repository and tool access verified), a uv-managed virtual environment, a deterministic archive install of a third-party pack, or a role-to-model mapping file."
