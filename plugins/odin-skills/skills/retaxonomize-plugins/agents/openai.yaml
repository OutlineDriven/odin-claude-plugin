interface:
  display_name: "Retaxonomize Plugins"
  short_description: "Use when skills must move between plugins, or plugins must be created, merged, or retired, in a marketplace whose manifests are generated from a catalog."
