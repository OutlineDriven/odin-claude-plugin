interface:
  display_name: "Trailmark Structural"
  short_description: "Use when a target needs a single-snapshot structural analysis: hotspots, entrypoints, taint, blast radius, privilege boundaries, proxies, subgraphs."
